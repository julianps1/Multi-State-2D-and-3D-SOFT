##Benchmark for Ferretti model

This folder contains the converged results for a simulation on a 256x256 grid for the 2-state Ferretti model. After compilation or modification this model can
be run to compare results and ensure no bugs were introduced. The input file to run the calculation is in INq, grid parameters are defiend in globals.hpp
